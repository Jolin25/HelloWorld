package com.jrl.learn;
/** knowledge point:
 * 包的学习：
 *  语法：
 *      1.package语句必须是位于第一行有效代码，用来表示我这个文件在哪个文件夹下
 *      2.import 包的全路径
 *  本质：
 *      1.包其实就代表文件夹，类代表文件
 *      2.Java只能看到和自己这个类在同一个包下的东西
 *  使用规则：
 *      在不同的包里可以由相同名字的类
 *
 */
public class LearnPackage {
}
